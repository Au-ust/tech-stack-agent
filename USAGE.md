# 使用指南

本文档提供详细的使用说明和常见场景示例。

## 目录

1. [首次使用](#首次使用)
2. [使用场景示例](#使用场景示例)
3. [高级配置](#高级配置)
4. [常见问题](#常见问题)

## 首次使用

### 步骤 1: 安装

```bash
# 克隆仓库
git clone <repository-url>
cd tech-stack-agent

# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 步骤 2: 配置 API Key

1. 访问 [Deepseek 平台](https://platform.deepseek.com/)
2. 注册账号并登录
3. 在 API Keys 页面创建新的 API Key
4. 复制 API Key

```bash
# 创建配置文件
cp .env.example .env

# 编辑 .env 文件，填入你的 API Key
# DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
```

### 步骤 3: 运行

```bash
python cli.py
```

## 使用场景示例

### 场景 1: 个人博客网站

**项目背景**:
- 项目类型: Web应用
- 团队规模: 1-3人（小型团队）
- 开发时间线: 1个月内
- 特殊需求: 需要SEO优化，支持Markdown，快速加载

**推荐技术栈**:
- 框架: Next.js 14 (App Router)
- 样式: Tailwind CSS
- 内容管理: MDX
- 部署: Vercel

**选型理由**:
- Next.js 提供优秀的SEO支持和静态生成能力
- Tailwind CSS 快速开发，无需CSS专家
- MDX 原生支持Markdown
- Vercel 免费托管，零配置部署

### 场景 2: 中型电商平台

**项目背景**:
- 项目类型: Web应用
- 团队规模: 4-10人（中型团队）
- 开发时间线: 3-6个月
- 特殊需求: 高性能、实时库存更新、支付集成

**推荐技术栈**:
- 框架: React 18 + Next.js
- 状态管理: Zustand
- UI组件: shadcn/ui
- 数据获取: TanStack Query
- 实时通信: WebSocket
- 测试: Vitest + Testing Library

**选型理由**:
- React生态成熟，第三方库丰富
- Zustand轻量级，适合复杂状态管理
- TanStack Query优化数据获取和缓存
- Next.js提供SSR和性能优化

### 场景 3: 企业后台管理系统

**项目背景**:
- 项目类型: Web应用
- 团队规模: 10人以上（大型团队）
- 开发时间线: 6个月以上
- 特殊需求: 权限管理、复杂表单、数据可视化

**推荐技术栈**:
- 框架: React 18 + TypeScript
- 路由: React Router v6
- 状态管理: Redux Toolkit
- UI组件: Ant Design
- 表单: React Hook Form + Zod
- 图表: Apache ECharts
- 构建: Vite
- 测试: Jest + Cypress

**选型理由**:
- TypeScript提供类型安全
- Redux Toolkit适合大型应用状态管理
- Ant Design组件丰富，符合企业UI规范
- 完整的测试方案保证代码质量

## 高级配置

### 自定义搜索策略

修改 `src/tools/search.py` 中的搜索参数：

```python
search_tool = TechSearchTool(
    max_results=10,  # 增加搜索结果数量
    delay=0.5,       # 减少搜索延迟
)
```

### 调整 LLM 参数

在 `.env` 文件中配置：

```bash
# 更高的温度值会产生更有创意的输出
DEEPSEEK_TEMPERATURE=0.8

# 增加最大token数以获得更详细的文档
DEEPSEEK_MAX_TOKENS=6000
```

### 自定义文档模板

编辑 `src/templates/tech_doc_template.md` 以修改文档结构。

### 添加新的问题节点

在 `src/agent/nodes.py` 中添加：

```python
def ask_budget_node(state: TechStackState) -> Dict[str, Any]:
    """询问项目预算"""
    budget = Prompt.ask(
        "请选择项目预算范围",
        choices=["低预算", "中等预算", "高预算"],
    )
    return {
        "budget": budget,
        "messages": [f"预算: {budget}"]
    }
```

然后在 `src/agent/graph.py` 中将节点添加到工作流。

## 常见问题

### Q: 如何处理API调用失败？

**A**: Agent 内置了错误处理机制：
- 分析失败会使用默认配置
- 搜索失败会跳过在线调研，使用LLM已有知识
- 确保 `.env` 文件正确配置API Key

### Q: 生成的文档在哪里？

**A**: 文档保存在 `outputs/` 目录下，文件名格式：
```
tech_stack_{项目类型}_{时间戳}.md
```

### Q: 如何提高搜索质量？

**A**: 
1. 在"特殊需求"中提供更多细节
2. 修改 `src/prompts/searcher.py` 优化搜索关键词生成
3. 增加搜索结果数量（修改 `search.py` 中的 `max_results`）

### Q: 文档太长或太短？

**A**: 调整 `.env` 中的 `DEEPSEEK_MAX_TOKENS` 参数：
- 增加值 → 更详细的文档
- 减少值 → 更简洁的文档

### Q: 如何批量生成文档？

**A**: 创建脚本批量运行：

```python
# batch_generate.py
from src.agent.graph import get_workflow_app

projects = [
    {
        "project_type": "Web应用",
        "team_size": "1-3人",
        "timeline": "1个月内",
        "special_requirements": "博客网站",
    },
    {
        "project_type": "移动应用",
        "team_size": "4-10人",
        "timeline": "3-6个月",
        "special_requirements": "社交应用",
    },
]

app = get_workflow_app()

for project in projects:
    # 初始化完整状态
    state = {**project, **other_required_fields}
    result = app.invoke(state)
    print(f"✓ 已生成: {result['output_path']}")
```

### Q: 能否支持其他语言？

**A**: 可以通过修改提示词支持其他语言：
1. 编辑 `src/prompts/*.py` 中的提示词
2. 将中文替换为目标语言
3. 确保LLM支持该语言

### Q: 如何集成到现有系统？

**A**: 作为Python模块导入：

```python
from src.agent.graph import get_workflow_app

def generate_tech_doc(project_info):
    app = get_workflow_app()
    result = app.invoke(project_info)
    return result["final_document"]
```

### Q: 性能优化建议？

**A**:
1. 禁用搜索（修改分析节点返回 `needs_search: False`）
2. 使用更小的 `max_tokens` 值
3. 减少搜索关键词数量
4. 使用缓存机制存储常见查询结果

## 获取帮助

- GitHub Issues: 报告问题
- GitHub Discussions: 功能讨论
- 文档: 查看 README.md

## 更新日志

### v0.1.0 (2026-02-17)
- ✨ 初始版本发布
- 🎯 引导式问答流程
- 🔍 DuckDuckGo 搜索集成
- 📝 完整文档生成
- 💰 Deepseek API 集成（超低成本）
